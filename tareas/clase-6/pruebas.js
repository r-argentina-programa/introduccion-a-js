function probarValidarFamiliares() {
    console.assert(
        validarFamiliares(0) === 'Debe ingresar por lo menos 1 integrante en la familia',
        'Validar familiares no validó que se haya ingresado por lo menos 1 integrante en la familia'
    )

}
